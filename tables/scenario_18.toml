[table]
name = "scenario_18"
type = "pointer"

[encoding]
table_file = "tables/rbshura_br_pt.tbl"
terminator = 0xFF

[pointers]
# 24-bit LE HiROM pointers (45 entries). Widened 2026-05-17 — see
# [[project_24bit_ptr_methodology]]. [data] block below is informational
# only; the relocate handler routes every entry to [rom.build].freespace.
offset = "$05B8EF"
count = 45
size = 3

[data]
# Bank $85 upper-half ROM region. Permissive bounds — scenarios
# share strings within a bank; tighten when build-side ownership matters.
offset = "$05B949"
end = "$05BD95"

[section]
kind = "script"
[section.placement]
mode = "relocate"
