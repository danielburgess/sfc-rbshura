[table]
name = "scenario_10"
type = "pointer"

[encoding]
table_file = "tables/rbshura_br_pt.tbl"
terminator = 0xFF

[pointers]
# 24-bit LE HiROM pointers (32 entries). Widened 2026-05-17 — see
# [[project_24bit_ptr_methodology]]. [data] block below is informational
# only; the relocate handler routes every entry to [rom.build].freespace.
offset = "$07879C"
count = 32
size = 3

[data]
# Bank $87 upper-half ROM region. Permissive bounds — scenarios
# share strings within a bank; tighten when build-side ownership matters.
offset = "$0787DC"
end = "$078A13"

[section]
kind = "script"
[section.placement]
mode = "relocate"
