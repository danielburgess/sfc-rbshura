[table]
name = "scenario_12"
type = "pointer"

[encoding]
table_file = "tables/rbshura_en.tbl"
terminator = 0xFF

[pointers]
# 16-bit LE pointers in HiROM bank $87 (29 entries).
offset = "$878C5A"
count = 29
size = 2

[data]
# Bank $87 upper-half ROM region. Permissive bounds — scenarios
# share strings within a bank; tighten when build-side ownership matters.
offset = "$878C94"
end = "$878E39"

[section]
kind = "script"
[section.placement]
mode = "relocate"
